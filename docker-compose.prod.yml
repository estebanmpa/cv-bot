name: cv-bot-prod

services:
  cv-bot:
    container_name: cv-bot-container
    build:
      context: .
      dockerfile: Dockerfile.prod
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "3000"
    environment:
      PORT: 3000
